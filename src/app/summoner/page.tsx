"use client";

import UserInfo from "@/components/userInfo";
import React from "react";

export default function SummonerPage() {
  return (
    <main className="mt-5">
      <UserInfo />
    </main>
  );
}
